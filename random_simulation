# 状態の生成
state = State()

# ゲーム終了までのループ
count = 0
while True:
  # ゲーム終了時
  count += 1
  if state.is_done():
    break;
  if count > 100:
    break;

  # 行動の取得
  action = random_action(state)
  
  # 次の状態の取得
  state = state.next(action)

  # 文字列表示
  print(state)
  print()
